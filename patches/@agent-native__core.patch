diff --git a/dist/action.js b/dist/action.js
index bc89881c0b6dd98ede11663ce4cfed215684f09a..e192bbd242d69345b8f1a2d816a20ada83851142 100644
--- a/dist/action.js
+++ b/dist/action.js
@@ -345,6 +345,14 @@ function stripUnsupportedSchemaKeywords(node) {
         return node;
     const obj = node;
     delete obj.propertyNames;
+    // OpenAI's structured-output validator rejects regex lookarounds in
+    // `pattern` (400: "regex lookaround is not supported"); Zod v4 emits them
+    // for z.string().email(). Anthropic ignores `pattern`, so dropping only
+    // the unsupported ones keeps action schemas portable; `format` remains
+    // and runtime Zod validation still enforces the real constraint.
+    if (typeof obj.pattern === "string" && /\(\?<?[=!]/.test(obj.pattern)) {
+        delete obj.pattern;
+    }
     for (const key of SUBSCHEMA_VALUE_KEYS) {
         // `items`/`additionalItems` may also be an array of subschemas.
         const value = obj[key];
diff --git a/dist/server/agent-chat-plugin.js b/dist/server/agent-chat-plugin.js
index 6f5f3472c66c4886b4a1a56fce90feaeba0daf3e..4c9b3b0644b42f2e19eac5debca49406205a3667 100644
--- a/dist/server/agent-chat-plugin.js
+++ b/dist/server/agent-chat-plugin.js
@@ -2091,8 +2091,8 @@ export function createAgentChatPlugin(options) {
                                 : undefined,
                         });
                         if (!assistantMsg) {
-                            // No content produced — just bump timestamp
-                            await updateThreadData(threadId, thread.threadData, thread.title, thread.preview, thread.messageCount);
+                            // No content produced — the timestamp is deliberately preserved;
+                            // finalizing a contentless run is not user activity.
                             return;
                         }
                         // Parse existing thread_data, append assistant message only if
@@ -4462,7 +4462,13 @@ Non-code requests are still fine on this surface: read data, navigate the UI, su
                                     // Invalid JSON — keep the title supplied by the client.
                                 }
                             }
-                            await updateThreadData(threadId, newThreadData, nextTitle, nextPreview, newMessageCount, { ignoreConflicts: true });
+                            const threadDataUnchanged = newThreadData === thread.threadData &&
+                                nextTitle === thread.title &&
+                                nextPreview === thread.preview &&
+                                newMessageCount === thread.messageCount;
+                            if (!threadDataUnchanged) {
+                                await updateThreadData(threadId, newThreadData, nextTitle, nextPreview, newMessageCount, { ignoreConflicts: true });
+                            }
                             // Scope updates piggyback on the PUT — the client uses this
                             // path for both "detach" (scope: null) and "retag" flows.
                             // Send the field as `scope: undefined` (or omit it) when
diff --git a/dist/client/AssistantChat.js b/dist/client/AssistantChat.js
--- a/dist/client/AssistantChat.js
+++ b/dist/client/AssistantChat.js
@@ -2654,7 +2654,31 @@
                     const res = await fetch(`${apiUrl}/threads/${encodeURIComponent(threadId)}`);
                     if (!res.ok) {
                         if (!cancelled) {
-                            setThreadRestoreError(res.status === 404 ? "not-found" : "unavailable");
+                            // A 404 for a thread with no local or cached content is a
+                            // ghost id: a client-minted tab persisted to localStorage
+                            // before its first message created the server row. Treat it
+                            // as a fresh empty chat — the first message creates the row
+                            // (persistSubmittedUserMessage), same as any new tab —
+                            // instead of a dead-end "Restore request failed" card whose
+                            // Retry can never succeed.
+                            if (res.status === 404) {
+                                let hasLocalMessages = false;
+                                try {
+                                    hasLocalMessages = (normalizeThreadRepository(threadRuntime.export())?.messages?.length ?? 0) > 0;
+                                }
+                                catch {
+                                    // Export/normalize failure — fall through to the
+                                    // visible error surface rather than hiding content.
+                                }
+                                if (!hasLocalMessages && !initialCachedThreadSnapshot) {
+                                    setThreadRestoreError(null);
+                                    return;
+                                }
+                                setThreadRestoreError("not-found");
+                            }
+                            else {
+                                setThreadRestoreError("unavailable");
+                            }
                         }
                         return;
                     }
