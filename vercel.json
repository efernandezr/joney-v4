{
  "buildCommand": "npx @agent-native/core@latest deploy --preset vercel --build-only && node scripts/compress-vercel-routes.mjs && if [ \"$VERCEL_ENV\" = \"production\" ]; then pnpm -r --workspace-concurrency=1 --if-present run migrate:production; fi",
  "installCommand": "pnpm install",
  "framework": null
}
