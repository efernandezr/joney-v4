# React Router generated types
.react-router/

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*

node_modules
build/
dist
dist-ssr
.output/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*
!.env.example

# Data
data/uploads/
data/settings.json
data/.sessions.json

# Learnings (personal preferences and memory — use learnings.defaults.md for tracked defaults)
learnings.md

.vercel/

# Playwright E2E
/test-results/
/playwright-report/
/e2e/.auth/
data/e2e.db
data/e2e.db-*
.agent-native/

# .generated/bridge is COMMITTED (Vercel/CI fresh clones need it — same lesson
# as packages/creative-context/dist) and also regenerated on every build via
# the codegen:bridge step chained into the build script.
